SET @q := CONCAT('%', ? , '%'); -- the search term
SELECT id, first_name, last_name, email, phone, created_at
FROM contacts
WHERE user_id = ?
  AND (first_name LIKE @q OR last_name LIKE @q OR email LIKE @q)
ORDER BY created_at DESC
LIMIT ? OFFSET ?;
